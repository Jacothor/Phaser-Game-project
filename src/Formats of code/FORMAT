// STAT BONUS
// {
//   type: "stat",
//   stat: "attack",
//   mode: "add", // add | mult
//   value: 2
// }

// TRIGGER BONUS
// {
//   type: "trigger",
//   trigger: "onHit", // onHit, onMiss, onKill, etc
//   effect: "heal",
//   value: 1,
//   chance: 0.25
// }

// CONVERSION BONUS
// {
//   type: "conversion",
//   effect: "heal",
//   mode: "mult",
//   value: 1.5
// }

// APPLy FORMAT OF BONUS SYSTEM IN COMBAT
// const livePlayerStats = this.playerState.getStats();

// this.currentBattle.player.attack = this.bonusSystem.getStat(
//   "attack",
//   livePlayerStats.attack
// );

// this.currentBattle.player.defense = this.bonusSystem.getStat(
//   "defense",
//   livePlayerStats.defense
// );

// this.currentBattle.player.speed = this.bonusSystem.getStat(
//   "speed",
//   livePlayerStats.speed
// );

// this.currentBattle.player.accuracy = this.bonusSystem.getStat(
//   "accuracy",
//   livePlayerStats.accuracy
// );

// this.currentBattle.player.evasion = this.bonusSystem.getStat(
//   "evasion",
//   livePlayerStats.evasion
// );

// this.currentBattle.player.maxHealth = this.bonusSystem.getStat(
//   "maxHealth",
//   livePlayerStats.maxHealth
// );

// FOR DAMAGE
// const baseDamage = this.calculateDamage(attacker, defender);
// const finalDamage = Math.max(
//   1,
//   Math.floor(this.bonusSystem.modifyValue("damage", baseDamage, {
//     attackerId: attacker.id,
//     defenderId: defender.id
//   }))
// );

// ON HIT
// const triggerEffects = this.bonusSystem.applyTriggers("onHit", {
//   attackerId: attacker.id,
//   defenderId: defender.id
// });

// for (const effect of triggerEffects) {
//   if (effect.kind === "heal" && attacker.id === "player") {
//     this.playerState.heal(effect.value ?? 0);
//   }
// }